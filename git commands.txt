# Check current global user information
git config --global user.name
git config --global user.email

# Set or update global user information
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"


# Set or update user information for a specific repository
git config user.name "Your Name"
git config user.email "your.email@example.com"




git push --force origin master


git push -u origin master

git push origin master

git pull origin master

git pull origin master --allow-unrelated-histories



git init
git add README.md
git remote add origin https://github.com/WajidHussain12/Disaster-Management-System_.git	
git branch -M master
git branch -m master main
git fetch origin
git branch -u origin/main main
git remote set-head origin -a
git push -u origin master
echo "" > .gitignore
git status





See git All Commands

git remote -v


update the URL of an existing remote, you can use the following command:

git remote set-url <remote-name> <new-remote-url>


